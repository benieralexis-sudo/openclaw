# ═══════════════════════════════════════════════════════════════════
# Multi-tenant clients docker-compose template
# ═══════════════════════════════════════════════════════════════════
#
# STATUS: empty template — no active client containers
#
# Previously contained router-digidemat + router-digitestlab (archived
# 2026-04-22 to /opt/moltbot/backups/old-clients-2026-04/)
#
# This file is INTENTIONALLY empty. It will be repopulated when a new
# client is onboarded (e.g. router-fimmop, router-{client-id}).
#
# Each client container :
#   - Inherits read-only volumes from /app/gateway + /app/skills/*
#   - Has isolated /data/* volumes (per client)
#   - Uses its own .env file at clients/{client-id}/.env
#   - Is routed by Caddy at {client-id}.ifind.fr (see gateway/caddy/)
#
# See gateway/caddy/README.md + Phase B memory docs for onboarding process.
# ═══════════════════════════════════════════════════════════════════

services: {}

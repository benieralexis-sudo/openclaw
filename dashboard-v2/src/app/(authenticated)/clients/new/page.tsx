import { CreateClientWizard } from "@/components/clients/create-client-wizard";

export const metadata = {
  title: "Nouveau client — iFIND",
};

export default function NewClientPage() {
  return <CreateClientWizard />;
}

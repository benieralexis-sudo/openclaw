import { SettingsBoard } from "@/components/settings/settings-board";

export const metadata = {
  title: "Paramètres — iFIND",
};

export default function SettingsPage() {
  return <SettingsBoard />;
}
